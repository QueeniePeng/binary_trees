Gem::Specification.new do |s|
  s.authors = ['Queenie Peng']
  s.files = Dir['lib/**/*.rb']
  s.name = 'binary_trees'
  s.summary = 'Convert a binary tree to array and vice versa. Given a tree and can sum all the tree node values.'
  s.version = '0.0.1'
  s.homepage = 'https://github.com/QueeniePeng/binary_trees'
end
